import classNames from "classnames/bind";

import Wrapper from "../../component/Wrapper";

const cx = classNames.bind("");

function Templates() {
  return <Wrapper header={"Template"}></Wrapper>;
}

export default Templates;
